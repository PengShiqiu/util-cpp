INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR})

ADD_EXECUTABLE(SkiplistTest SkiplistTest.cpp)

SET(EXECUTABLE_OUTPUT_PATH  ${PROJECT_SOURCE_DIR}/bin/tests)

TARGET_LINK_LIBRARIES(SkiplistTest util)

ADD_DEPENDENCIES(SkiplistTest util)
